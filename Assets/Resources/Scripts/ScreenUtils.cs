using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class ScreenUtils 
{
    public const float ScreenWidth = 7.5f;
    public const float ScreenHeight = 13.34f;

    public const float BottomScreen = - ScreenHeight / 2;
    public const float TopScreen = ScreenHeight / 2;
    public const float RightScreen = ScreenWidth / 2;
    public const float LeftScreen = - ScreenWidth / 2;

    public const int CamPositionZ = -10;
}
